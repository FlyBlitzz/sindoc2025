<form method="post" action="{{ path('app_livre_app_livre_delete', {'id': livre.idlivre}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ livre.idlivre) }}">
    <button class="btn">Delete</button>
</form>
