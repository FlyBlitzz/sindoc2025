{% extends 'base.html.twig' %}

{% block title %}Index{% endblock %}

{% block body %}
<head>
       <link rel="stylesheet" href="{{ asset('css/ajoutFiche.css') }}">

</head>
 <div class=" classs">
    <h1>Détails Index</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ index.idindex}}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ index.nom }}</td>
            </tr>
        </tbody>
    </table>
 <a href="{{ path('index_app_index_edit', {'id': index.idindex}) }}" class="btn btn-primary">Modifier</a><br /><br />
    <a href="{{ path('index_app_index_index') }}">retourner à la liste</a>




    </div>
{% endblock %}
